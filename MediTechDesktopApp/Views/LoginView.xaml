<UserControl x:Class="MediTechDesktopApp.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#FFF5F5F5">
        <Border Padding="20" CornerRadius="10" Background="White" ShadowEffect>
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="Login" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>
                <TextBox x:Name="txtUsername" Width="250" Height="30" PlaceholderText="Username" Margin="0,0,0,10"/>
                <PasswordBox x:Name="txtPassword" Width="250" Height="30" Margin="0,0,0,10"/>
                <Button Content="Login" Width="250" Height="30" Click="Login_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
